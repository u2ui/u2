# U2 Audit TODO

Stand: 2026-08-31. Zwei statische projektweite Audit-Durchgänge plus wenige Chromium-Smoke-Checks. Die Liste ist nach Risiko priorisiert. Bereits vorhandene, nicht zu diesem Audit gehörende Änderungen im RTE-Bereich wurden nicht angefasst.

## P0 – Fehler, Datenverlust und Sicherheit

- [ ] **Kataloggenerator atomar und deterministisch machen:** `update.repos.json.js` startet alle Kategorien mit einem nicht erwarteten `forEach(async ...)` parallel und überschreibt `u2/projects.json` nach jeder einzelnen Kategorie. Ein Lauf kann dadurch einen unvollständigen Katalog publizieren. Erst alles lesen, validieren und danach genau einmal via temporärer Datei ersetzen. [`u2/tools/update.repos.json.js`](u2/tools/update.repos.json.js#L11)
- [ ] **Generator darf nicht nebenbei alle READMEs umschreiben:** Katalogerzeugung, README-Generierung und Dateianlage trennen; Dry-run und Diff-Check ergänzen. Der aktuelle naive Markdown-Splitter kann Inhalte verlieren oder umsortieren. [`u2/tools/update.repos.json.js`](u2/tools/update.repos.json.js#L41)
- [ ] **Externes SVG vor DOM-Injektion sanitizen:** `u2-ico` schreibt beliebig konfigurierbares, per `fetch()` geladenes SVG direkt in `innerHTML`. Scripts, Event-Attribute, `foreignObject`, externe Referenzen und gefährliche URLs müssen entfernt oder SVGs als Bild/Mask geladen werden. [`el/ico/ico.js`](el/ico/ico.js#L72)
- [ ] **TOC ohne HTML-String bauen:** Heading-Text und daraus abgeleitete IDs landen ungeescaped in `innerHTML`; Text wie `<img ...>` wird wieder zu Markup. `a`, `href` und `textContent` per DOM-API setzen und IDs eindeutig/URL-sicher erzeugen. [`el/toc/toc.js`](el/toc/toc.js#L60)
- [ ] **AI-Ausgabe des alten RTE als untrusted behandeln:** Remote-Modellantworten werden ungeprüft als HTML angezeigt und in den Editor übernommen. Sanitizer/Allowlist vor Vorschau, Diff und Commit einschalten; Fehler-, Timeout- und Abbruchpfad ergänzen. [`js/rte0/src/ui/items/ai.js`](js/rte0/src/ui/items/ai.js#L75)
- [ ] **AI-Funktion nicht mit Platzhalter-Key ausliefern:** Endpoint, Modell und Credential-Injektion gehören in eine explizite Konfiguration bzw. einen Server-Proxy; niemals ein Browser-Bearer-Token im Quelltext vorsehen. [`js/rte0/src/ui/items/ai.js`](js/rte0/src/ui/items/ai.js#L118)
- [ ] **Service Worker nur für `GET` cachen:** Aktuell werden auch andere HTTP-Methoden abgefangen; `cache.put()` kann dann eine erfolgreiche Netzwerkantwort in einen Fehler/Fallback verwandeln. Navigation, Range-Requests und opaque Responses gezielt behandeln. [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L58)
- [ ] **Service-Worker-Ablaufzeit wirklich speichern:** `isCacheExpired()` liest `sw-cached-at`, aber `saveToCache()` setzt diesen Header nie. Damit laufen Cache-Einträge effektiv nicht ab. [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L140)
- [ ] **Offline-Fallback korrekt awaiten:** `caches.match('/offline.html') || new Response(...)` testet nur das immer truthy Promise; ein Cache-Miss kann dadurch `undefined` an `respondWith()` liefern. [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L165)
- [ ] **Cache-Löschung auf eigene Cache-Namen begrenzen:** `CLEAR_CACHE` löscht derzeit alle Caches des Origins; auch das Prefix-Cleanup kann fremde Apps treffen. Scope/App-ID in Cache-Namen aufnehmen und ausschließlich diese Namespaces verwalten. [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L42) [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L178)
- [ ] **Service Worker relativ zum Modul registrieren:** `navigator.serviceWorker.register('../sw.js')` wird relativ zur Dokument-URL aufgelöst und ist je nach Seite falsch. `new URL('./sw.js', import.meta.url)` verwenden; Default-Scope mit `Service-Worker-Allowed` abstimmen. [`js/serviceWorker/serviceWorker.js`](js/serviceWorker/serviceWorker.js#L14)
- [ ] **Navigator darf keine halbe Navigation darstellen:** Er ersetzt nur den Body; Head, Styles, Metadaten und Seitenscripts werden weder synchronisiert noch ausgeführt. Einen klaren Navigation-Vertrag definieren oder bei nicht kompatiblen Dokumenten auf native Navigation zurückfallen. [`js/navigator/navigator.js`](js/navigator/navigator.js#L184)
- [ ] **Navigator-Rennen abbrechen:** Mehrere schnelle Klicks/Back-Navigationen können out of order auflösen und eine alte Seite zuletzt einblenden. Pro Navigation AbortController/Sequenz-ID verwenden. [`js/navigator/navigator.js`](js/navigator/navigator.js#L267)
- [ ] **Persistentes HTML-Caching opt-in und benutzersicher machen:** Vollständige personalisierte Seiten inklusive Tokens/Inhalten werden drei Stunden in CacheStorage/localStorage gehalten. Cache-Control/`no-store`, Auth-Zustand, Logout/Purge, Versions-/Tenant-Namespace und maximale Größe respektieren. [`js/navigator/navigator.js`](js/navigator/navigator.js#L23)
- [ ] **Kaputten `u2-out`-Import korrigieren und entkoppeln:** `../../../../nuxodin/item.js/` zeigt von `el/out/` auf `/var/www/nuxodin/...`, nicht auf den vorhandenen Nachbar-Checkout. Ein publiziertes CDN-Modul darf ohnehin nicht von einer zufälligen Monorepo-Nachbarschaft abhängen. [`el/out/out.js`](el/out/out.js#L1)

## P1 – Loader, Framework-Kern und Laufzeit

- [ ] **Dynamische Attribut-/Klassenänderungen beobachten:** `enhance()` beobachtet nur hinzugefügte Nodes. Wird `u2-*` oder `class="u2-*"` später gesetzt, wird das Modul nicht geladen. `attributes` mit passendem Filter/Delta behandeln. [`u2/enhance.js`](u2/enhance.js#L125)
- [ ] **Fehlgeschlagene Loads erneut erlauben:** IDs werden vor `fetch/import` dauerhaft in `seen` geschrieben. Ein temporärer Netzfehler oder zu früher Katalogzugriff kann in diesem Root nie wieder heilen. Pending/loaded/failed getrennt modellieren. [`u2/enhance.js`](u2/enhance.js#L59)
- [ ] **Attribut-Module in Shadow Roots korrekt routen:** Der Kommentar behauptet, Dokument-Listener würden Shadow-Grenzen überschreiten. Events werden jedoch retargeted; viele Module arbeiten mit `e.target.closest(...)` und finden interne Attribute nicht. `composedPath()`, root-lokale Installation oder einen expliziten Attr-Adapter verwenden. [`u2/enhance.js`](u2/enhance.js#L85)
- [ ] **Unbekannte `u2-*`-Klassen ohne Konsolenrauschen behandeln:** Der Haupt-Smoke-Test meldet unter anderem `class/parallax-bg-stage` und `class/input`. Entweder Namespace strikt bereinigen oder vor der Warnung erkennen, ob es bewusst anwendungsinterne/Legacy-Klassen sind. [`u2/enhance.js`](u2/enhance.js#L66) [`u2/tests/test.html`](u2/tests/test.html#L56)
- [ ] **Stylesheet-Fehler propagieren:** `addCss()`/Queue werden nicht zurückgegeben oder abgefangen; `onLoad` wird schon vor erfolgreichem Laden gemeldet. Einen `ready`-Vertrag anbieten und Rejections sichtbar/kontrollierbar machen. [`u2/enhance.js`](u2/enhance.js#L41)
- [ ] **Katalog-Prefetch ohne unhandled rejection:** Das nackte `repos().then(...)` braucht einen definierten Fehlerzustand; zugleich sollten Status und Content-Type von `projects.json` validiert werden. [`u2/enhance.js`](u2/enhance.js#L21) [`u2/u2.js`](u2/u2.js#L17)
- [ ] **`importCss()` pro Root statt global deduplizieren:** Der globale URL-Key verhindert, dass dasselbe CSS in einem zweiten Document/ShadowRoot geladen wird. Root plus normalisierte URL und relevante Optionen (`media`) bilden den Cache-Key. [`u2/utils.js`](u2/utils.js#L4)
- [ ] **Keine `<div>`-Wrapper in `<head>` einfügen:** Chromium zeigt die globalen Styles aktuell in einem invaliden `head > div > link`-Baum. Links direkt an einen gültigen Container hängen oder eine standardskonforme Gruppierung nutzen. [`u2/utils.js`](u2/utils.js#L52)
- [ ] **Relative CSS-URLs nicht aus Error-Stacks erraten:** Stack-Format und Zeilenposition unterscheiden sich zwischen Engines/Minifizierung. Die Basis-URL explizit als Parameter oder via `new URL(path, import.meta.url)` am Aufrufer übergeben. [`u2/utils.js`](u2/utils.js#L16)
- [ ] **CSS-Load-Handler aufräumen und Fehlerdetails liefern:** `onload/onerror` nach Abschluss lösen, `Error` mit URL erzeugen und bei einem fehlgeschlagenen Link einen Retry ermöglichen. [`u2/utils.js`](u2/utils.js#L5)
- [ ] **Reduced-Motion-Mediaquery korrigieren:** `media="prefers-reduced-motion"` ist keine gültige Media Condition. `(prefers-reduced-motion: reduce)` verwenden; dasselbe gilt für die `@import`-Shorthands in den `full.css`-Dateien. [`u2/auto.js`](u2/auto.js#L48) [`css/base/full.css`](css/base/full.css#L4) [`css/classless/full.css`](css/classless/full.css#L4)
- [ ] **`sheet()` muss HTTP-Fehler ablehnen:** Ein 404-HTML wird derzeit als CSS geparst und als gecachte Erfolgs-Promise behalten. `res.ok`/Content-Type prüfen und rejected Einträge aus dem Cache entfernen. [`u2/Element.js`](u2/Element.js#L38)
- [ ] **Basis-URL für CSS-Ressourcen erhalten:** Für per `replaceSync()` konstruierte Stylesheets ist zu klären/testen, wie relative `url()` aufgelöst werden. Entweder relative Ressourcen verbieten+linten oder URLs gegen die Quell-CSS-URL absolut machen. [`u2/Element.js`](u2/Element.js#L40)
- [ ] **`useEl()` muss `extraElements` ebenfalls registrieren:** `enhance()` kann Zusatz-Tags registrieren, `U2Element.useEl()` nicht. Beide Ladewege brauchen denselben Vertrag. [`u2/Element.js`](u2/Element.js#L29)
- [ ] **Scoped Registry wirklich isolieren:** Moduldateien registrieren weiterhin global und `use()` importiert Attr-Module mit globalen Listenern. Fallback, Browser-Support und Verhalten mehrerer U2-Versionen in einem Dokument explizit testen und dokumentieren. [`u2/enhance.js`](u2/enhance.js#L15) [`u2/Element.js`](u2/Element.js#L7)
- [ ] **LocalStorage im Auto-Loader robust lesen/schreiben:** Ungültiges JSON, SecurityError und QuotaExceeded dürfen `DOMContentLoaded/pagehide` nicht sprengen. Schema/Version, Begrenzung und opt-out vorsehen. [`u2/auto.js`](u2/auto.js#L84)
- [ ] **Root-URL lesbar und sicher ableiten:** Die Konstruktion über `origin + pathname + '/../../'` ignoriert URL-Sonderfälle unnötig. Direkt `new URL('../', import.meta.url)` verwenden und Query/Hash testen. [`u2/auto.js`](u2/auto.js#L3)
- [ ] **Global-API ohne impliziten Identifier:** Nach `window.u2 ??=` wird unqualifiziert `u2.js = ...` geschrieben. Durchgehend `globalThis.u2`/exportierte Funktionen verwenden und Kollisionen/versionierte Instanzen definieren. [`u2/u2.js`](u2/u2.js#L3)

## P1 – Komponenten und Attribute

- [ ] **Tooltip-Shadow-Listener nicht bei jedem Mouseenter erneut anhängen:** Jeder Eintritt in einen Shadow-Host registriert weitere Listener. Roots in einer WeakSet deduplizieren oder zentral über `composedPath()` arbeiten. [`el/tooltip/tooltip.js`](el/tooltip/tooltip.js#L73)
- [ ] **Tooltip-ARIA tokenweise verwalten:** Das Element überschreibt bzw. entfernt das gesamte `aria-labelledby`, obwohl dort mehrere IDs oder eine fremde Beschriftung stehen können. Generierte ID nur hinzufügen/entfernen; mehrere IDREFs korrekt auflösen. [`el/tooltip/tooltip.js`](el/tooltip/tooltip.js#L16)
- [ ] **Tooltip-Popover-Modus vereinheitlichen:** `popover` wird direkt nacheinander auf `manual` und `hint` gesetzt. Gewünschten Modus und Browser-Fallback einmal festlegen. [`el/tooltip/tooltip.js`](el/tooltip/tooltip.js#L10)
- [ ] **Calendar-Observer speichern und disconnecten:** Jede erneute Verbindung erzeugt einen weiteren nicht erreichbaren MutationObserver. [`el/calendar/calendar.js`](el/calendar/calendar.js#L94)
- [ ] **Calendar-Datum ohne UTC/Lokal-Mischung:** `YYYY-MM-DD` wird als UTC geparst, Rendering und Monatsnavigation verwenden teils lokale Getter, teils UTC. Tests in positiven und negativen Zeitzonen ergänzen. [`el/calendar/calendar.js`](el/calendar/calendar.js#L86) [`el/calendar/calendar.js`](el/calendar/calendar.js#L101)
- [ ] **Calendar CSP-tauglich machen:** Inline-`onclick` im Shadow-Markup blockiert eine strikte CSP und mischt Verhalten in HTML. Listener in der Klasse registrieren. [`el/calendar/calendar.js`](el/calendar/calendar.js#L63)
- [ ] **Calendar-Locale validieren/fallbacken:** Ungültige `lang`-Werte können `Intl.Locale`/Formatter werfen; Fehler darf das Element nicht unbrauchbar machen. [`el/calendar/calendar.js`](el/calendar/calendar.js#L80)
- [ ] **Breadcrumb darf Links nicht destruktiv entwerten:** Beim Rendern wird dem aktuellen Link `href` entzogen; ein späteres Rendern kann URL, JSON-LD und Navigation nicht sauber wiederherstellen. `aria-current` reicht, oder Originalzustand reversibel verwalten. [`el/breadcrumb/breadcrumb.js`](el/breadcrumb/breadcrumb.js#L72)
- [ ] **Breadcrumb-URL-Listener beim Disconnect entfernen:** Der anonyme globale Listener hält Auto-Breadcrumbs nach Entfernung am Leben und verdoppelt sich beim Reconnect. AbortController verwenden. [`el/breadcrumb/breadcrumb.js`](el/breadcrumb/breadcrumb.js#L36)
- [ ] **Breadcrumb-Beschriftung vom Host übernehmen:** Der auskommentierte korrekte Ansatz wurde durch `nav.ariaLabel ??= ...` ersetzt; `aria-label` am Custom Element wird nicht gespiegelt. [`el/breadcrumb/breadcrumb.js`](el/breadcrumb/breadcrumb.js#L24)
- [ ] **Typewriter mit leerem/dynamischem Inhalt stabil machen:** `reset()` ruft auf `null.append(...)` auf, wenn kein Zeichen existiert; Konstruktor-Manipulation vor vollständigem Parserinhalt und nachträgliche Textänderungen brauchen einen Vertrag. [`el/typewriter/typewriter.js`](el/typewriter/typewriter.js#L30) [`el/typewriter/typewriter.js`](el/typewriter/typewriter.js#L94)
- [ ] **Typewriter-Zeichen mit `textContent` statt `innerHTML` setzen:** Literale `<`, `&` und ähnliche Zeichen werden aktuell als Markup interpretiert/verloren. [`el/typewriter/typewriter.js`](el/typewriter/typewriter.js#L124)
- [ ] **Typewriter-IntersectionObserver beim Disconnect trennen:** `pause()` stoppt nur den Timer; der Observer bleibt aktiv. Reconnect und `autoplay`-Toggle testen. [`el/typewriter/typewriter.js`](el/typewriter/typewriter.js#L41)
- [ ] **`u2-out`-API, README und Manifest synchronisieren:** Minimalbeispiel dokumentiert alte `value`/`digits`, Implementierung akzeptiert nur `item`, Manifest listet gar keine Attribute. [`el/out/README.md`](el/out/README.md#L4) [`el/out/out.js`](el/out/out.js#L13) [`el/out/manifest.json`](el/out/manifest.json#L11)
- [ ] **`u2-out` Fehlerzustände behandeln:** Leere/ungültige Pfade, unbekannte Treiber, Importfehler und Item-Lifecycle nicht als unhandled rejection enden lassen; laufenden Effect beim Pfadwechsel/Disconnect entsorgen. [`el/out/out.js`](el/out/out.js#L18)
- [ ] **QR-Code auf Inhaltsänderungen reagieren:** Nach `connectedCallback()` werden Textänderungen nicht beobachtet. Race/Fehlerzustand und maximale Payload definieren; Dependency nicht erst zur Laufzeit von einem Dritt-CDN holen müssen. [`el/qrcode/qrcode.js`](el/qrcode/qrcode.js#L24)
- [ ] **ICO-Lader gegen veraltete Antworten absichern:** Wechselt `icon` während ein Fetch läuft, kann die alte Antwort das neue Icon überschreiben. Sequenz-ID/AbortSignal verwenden; fehlgeschlagene Cache-Promises retrybar machen. [`el/ico/ico.js`](el/ico/ico.js#L64)
- [ ] **ICO-ViewBox-Fallback auf `NaN` prüfen:** `parseFloat(null) ?? 24` bleibt `NaN`; `Number.isFinite()` verwenden und Nicht-SVG-Antworten abweisen. [`el/ico/ico.js`](el/ico/ico.js#L83)
- [ ] **Pagination auf Containergröße reagieren:** Die Seitenzahl hängt von `clientWidth` ab, wird aber bei Resize nicht neu berechnet. ResizeObserver ergänzen sowie `current/total` clampen und ungültige Werte testen. [`el/pagination/pagination.js`](el/pagination/pagination.js#L34)
- [ ] **Responsive-Debugger entsorgbar machen:** `debug()` erstellt DOM plus 50-Hz-Interval ohne Handle/Cleanup; außerdem enthält `text-aligen` einen Tippfehler. Debug-Overlay als explizites Objekt mit `destroy()`. [`el/responsive/responsive.js`](el/responsive/responsive.js#L80)
- [ ] **Textfit für leeren/unsichtbaren Text absichern:** Division durch Breite 0 kann `Infinitypx`/`NaNpx` erzeugen. Pending Throttle/RAF bei Disconnect abbrechen und Font-Load via `document.fonts` statt nur `load` beobachten. [`el/textfit/textfit.js`](el/textfit/textfit.js#L51)
- [ ] **Input-Object-URLs widerrufen:** Preview-URLs aus `URL.createObjectURL()` nach Load, Entfernen, Neu-Rendern und Disconnect revoken. [`el/input/input.js`](el/input/input.js#L378)
- [ ] **Remote Input-Optionen robust laden:** `res.ok`, erwartetes JSON-Schema, Abort bei Source-Wechsel/Disconnect und out-of-order Responses behandeln. [`el/input/input.js`](el/input/input.js#L93)
- [ ] **Disableif außerhalb eines Forms nicht crashen lassen:** `el.form.elements` und `el.form.addEventListener` werden ohne Null-Prüfung verwendet. Zielauflösung definieren und Listener im `off`-Pfad entfernen. [`attr/disableif/disableif.js`](attr/disableif/disableif.js#L3) [`attr/disableif/disableif.js`](attr/disableif/disableif.js#L47)
- [ ] **Store robust und eindeutig machen:** Beschädigtes Storage-JSON darf nicht werfen; Inputs ohne `name`, mehrere Forms ohne ID, Radio-/Checkbox-Gruppen und gleichnamige globale Felder brauchen definierte Keys/Semantik. Restore sollte passende `input/change`-Events optional auslösen. [`attr/store/store.js`](attr/store/store.js#L3)
- [ ] **Href-Protokolle allowlisten und Shadow DOM unterstützen:** Nur `javascript:` zu blockieren ist zu schwach; erlaubte Navigationsschemata explizit definieren. `composedPath()` statt ausschließlich `e.target.closest()` nutzen. [`attr/href/href.js`](attr/href/href.js#L1)
- [ ] **Focusgroup-Selektor und Verschachtelung korrigieren:** `:not(u2-focusgroup)` meint ein Tag statt `[u2-focusgroup]`, und der gruppierte Selektor begrenzt nur den ersten Ast. Sichtbarkeit, `inert`, versteckte/disabled Items, Slots, RTL und verschachtelte Gruppen testen. [`attr/focusgroup/focusgroup.js`](attr/focusgroup/focusgroup.js#L70)
- [ ] **Parallax-Listener zentral drosseln und passiv machen:** Mehrere globale `scroll/wheel/resize/load`-Listener und RAFs können pro Event mehrfach Layout lesen/schreiben. Einen Scheduler verwenden und entfernte Elemente aus Registries lösen. [`attr/parallax/parallax.js`](attr/parallax/parallax.js#L59) [`el/parallax-bg/parallax-bg.js`](el/parallax-bg/parallax-bg.js#L40)
- [ ] **Carousel-/Tooltip-/Typewriter-Globalverhalten auf mehrere Instanzen und Shadow Roots testen:** Globale Listener dürfen nicht jede Instanz bei jedem Event durchsuchen oder fremde Komponenten steuern. Ereignisse so lokal wie möglich halten. [`el/carousel/carousel.js`](el/carousel/carousel.js#L337)
- [ ] **Splitpanel-Separator tastaturbedienbar machen:** `role=separator` braucht Fokus, `aria-orientation`, Wertattribute und Arrow-Key-Resize; Pointer-Capture allein reicht nicht. [`el/splitpanel/splitpanel.js`](el/splitpanel/splitpanel.js#L103)

## P1 – Navigator und Service Worker im Detail

- [ ] **Unbenutzten `morphdom`-Remoteimport entfernen oder wirklich verwenden:** Der Navigator blockiert sonst schon beim Import an `esm.sh`, obwohl die lokale `Morph`-Klasse ausgeführt wird. [`js/navigator/navigator.js`](js/navigator/navigator.js#L176)
- [ ] **Click-Interception browsergleich machen:** `metaKey`, `altKey`, mittlere/rechte Taste, `download`, `rel=external`, Contenteditable und bereits verhinderte Events berücksichtigen. [`js/navigator/navigator.js`](js/navigator/navigator.js#L353)
- [ ] **Fetch-Antwort vor Morph prüfen:** finalen Origin/Redirect, `Content-Type: text/html`, Fehlerdokumente und Login-Redirects validieren. [`js/navigator/navigator.js`](js/navigator/navigator.js#L229)
- [ ] **Failed-URL-Blacklist mit Retry/TTL:** Ein einzelner HTTP-Fehler sperrt die URL für die komplette Seitenlebensdauer. [`js/navigator/navigator.js`](js/navigator/navigator.js#L229)
- [ ] **Persistent Cache begrenzen und LRU statt FIFO:** `cacheSize` gilt nur im RAM; CacheStorage wächst unbegrenzt. Zugriff sollte Recency aktualisieren, alte Versionen müssen bereinigt werden. [`js/navigator/navigator.js`](js/navigator/navigator.js#L217)
- [ ] **Prefetch budgetieren:** Bis zu 13 Seiten pro Idle-Phase sind für Mobilfunk, `saveData`, schwache Geräte und serverseitige Last aggressiv. Hash-/Download-/no-prefetch-Links ausschließen, Concurrency und Bytebudget konfigurieren. [`js/navigator/navigator.js`](js/navigator/navigator.js#L289)
- [ ] **Navigator konfigurierbar und entsorgbar machen:** Feste Modul-Globals, Auto-Init und globale Listener verhindern mehrere Instanzen/Tests und sauberes Teardown. `createNavigator(options)`/`destroy()` anbieten. [`js/navigator/navigator.js`](js/navigator/navigator.js#L3)
- [ ] **Service-Worker-Responsegröße ohne Voll-Blob messen:** `res.clone().blob()` puffert jede Antwort komplett. Zuerst `Content-Length`, konfigurierbare Grenzen und Streaming-Verhalten nutzen; 100 KiB Default begründen. [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L151)
- [ ] **Push-Daten defensiv parsen und Ziel-URLs begrenzen:** `e.data.json()` kann werfen; Notifications sollten keine ungeprüften externen URLs öffnen. [`js/serviceWorker/sw.js`](js/serviceWorker/sw.js#L192)
- [ ] **Update-Polling nicht alle 30 Sekunden pro Register-Aufruf:** Registrierung deduplizieren, Intervall konfigurierbar machen/stoppen und `navigationPreload.enable()` gegen Support/Fehler absichern. [`js/serviceWorker/serviceWorker.js`](js/serviceWorker/serviceWorker.js#L23)
- [ ] **Listener-Subscriptions rückgabefähig machen:** `onUpdateAvailable()` und `onCacheServed()` sollten Unsubscribe-Funktionen liefern, damit Tests/SPAs keine Listener sammeln. [`js/serviceWorker/serviceWorker.js`](js/serviceWorker/serviceWorker.js#L37)

## P2 – Performance, CSS und Abhängigkeiten

- [ ] **Drittanbieter-Policy festlegen:** Produktionsmodule laden JavaScript direkt von jsDelivr, esm.sh, Skypack, unpkg und GitHub Pages; teils mit `@main`/ohne unveränderlichen Pin. Lock/Vendoring/Import-Map, CSP, Offline-Verhalten, SRI-Möglichkeit und Updateprozess definieren. Beispiele: [`attr/draghandle/draghandle.js`](attr/draghandle/draghandle.js#L1) [`attr/intersect/intersect.js`](attr/intersect/intersect.js#L52) [`u2/auto.ui.js`](u2/auto.ui.js#L1)
- [ ] **Chart-Engines vereinheitlichen:** Sieben Loader nutzen verschiedene CDNs, Versionen, CSS-Injektionsweisen und Exportformen; `chart.js.js` hat zudem einen inkonsistenten Dateinamen und unversionierten Skypack-Import. Gemeinsamen Adaptervertrag, Lazy-Cache, Fehler-/Abortpfad und Tests pro Engine schaffen. [`el/chart/engines`](el/chart/engines)
- [ ] **ApexCharts-Versionen konsistent machen:** JavaScript wird als 3.49.1 geladen, CSS als 5.3.6; die Kommentare enthalten noch Chat-/Entwurfsnotizen. [`el/chart/engines/apexcharts.js`](el/chart/engines/apexcharts.js#L1)
- [ ] **Google-Fonts nicht ungefragt aus Themes laden:** Externe Requests sind Datenschutz-, Offline- und CSP-relevant. System-Fallback oder opt-in Font-Pakete anbieten; `theme/soft` importiert Roboto doppelt. [`theme/soft/theme.css`](theme/soft/theme.css#L1)
- [ ] **`theme/tmp` und `theme/test` nicht als reguläre Module publizieren:** In stabile Namen überführen, als Experimente kennzeichnen oder aus Katalog/NPM-Dateien ausschließen. [`theme/tmp`](theme/tmp) [`theme/test`](theme/test)
- [ ] **Layout-Hotpaths messen:** Parallax, Masonry, Carousel, Table, Textfit und Responsive kombinieren Resize-/MutationObserver mit synchronen Geometrie-Lese-/Schreibzyklen. Pro Komponente Read/compute/write phasen, RAF-Coalescing und Benchmarks für viele Instanzen ergänzen.
- [ ] **Auto-Scanner benchmarken:** Bei großen eingefügten Subtrees werden alle Elemente, Klassen und Attribute synchron besucht. Mutation-Batches deduplizieren und Grenzwerte/Idle-Aufteilung mit realistischen DOM-Größen messen. [`u2/enhance.js`](u2/enhance.js#L97)
- [ ] **Ungültige deaktivierte CSS-Deklarationen bereinigen:** `xmax-height`, `xbackground`, `xbox-shadow`, `text-aligen` usw. erzeugen Lint-Rauschen. Entscheidungen als Kommentare/Experimente festhalten statt Fantasie-Properties im Produktions-CSS. Beispiele: [`class/table/table.css`](class/table/table.css#L96) [`class/card/card.css`](class/card/card.css#L28) [`css/classless/more.css`](css/classless/more.css#L35)
- [ ] **Fokusdarstellung auditieren:** Mehrere Komponenten setzen `outline: 0/none`; sicherstellen, dass jeder Tastaturfokus einen mindestens gleichwertigen `:focus-visible`-Ersatz hat. Beispiele: [`el/tabs/tabs.css`](el/tabs/tabs.css#L13) [`el/input/input.css`](el/input/input.css#L34) [`el/code/code.js`](el/code/code.js#L35) [`el/rating/rating.js`](el/rating/rating.js#L23)
- [ ] **Hardcodierte Farben durch Tokens ersetzen:** Calendar, Tour, Alert, Tree, Drawer und weitere Shadow-Styles verwenden feste Schwarz-/Weißwerte und passen sich Themes/Dark/Forced-Colors nicht durchgehend an. Kontrast- und forced-colors Tests ergänzen.
- [ ] **Cascade-Layer/Override-Vertrag dokumentieren:** Norm, Base, Classless, Themes, Komponenten und Anwender-CSS brauchen eine reproduzierbare Reihenfolge; Loader und `adoptedStyleSheets.unshift/push` verhalten sich unterschiedlich. [`u2/Element.js`](u2/Element.js#L18) [`u2/enhance.js`](u2/enhance.js#L41)
- [ ] **`css/utils` entweder fertigstellen oder nicht als stabiles Modul listen:** Katalog/Skill bezeichnen es als `planned`, obwohl es publiziert wird. [`css/utils/README.md`](css/utils/README.md#L1)

## P2 – Tests, CI, Packaging und Dokumentation

- [ ] **Einen zentralen Testbefehl einführen:** Root-`package.json` hat keine Scripts; die vielen HTML-Seiten sind überwiegend Demos ohne maschinenlesbare Assertions. Ein Runner soll Unit-, Browser-, Manifest-, Link-, JSON- und CSS-Prüfungen mit eindeutigem Exitcode ausführen. [`package.json`](package.json#L1)
- [ ] **CI für Chromium, Firefox und WebKit einrichten:** Mindestens Loader, Custom-Element-Lifecycle, Tastatur/ARIA, Shadow DOM, Form-Participation und CSP-Szenarien automatisieren; externe Netzzugriffe in Tests mocken.
- [ ] **Schema-Checks verpflichtend machen:** Alle JSON-Dateien, `projects.json`, Custom Elements Manifests und generiertes `SKILL.md` gegen Dateisystem/API validieren; ungültige oder redundante Unter-Package-Dateien bereits vor Veröffentlichung abweisen. [`package.json`](package.json#L1)
- [ ] **Katalog-Reproduzierbarkeit testen:** Generatorlauf darf bei unverändertem Tree keinen Diff erzeugen; jedes Modul muss exakt einmal gelistet sein und `css/js`-Flags müssen mit Dateien übereinstimmen. Die aktuellen uncommitted RTE2-Verzeichnisse fehlen erwartbar noch im generierten Katalog. [`u2/projects.json`](u2/projects.json#L1)
- [ ] **NPM-Paket als ESM klar deklarieren:** `type`, `exports`, Einstiegspunkte und Side Effects definieren; prüfen, ob `old/`, Debug-Dateien, Fonts und Experimente wirklich im publizierten Paket landen sollen. [`package.json`](package.json#L1)
- [ ] **Root-README-Browsermatrix aktualisieren:** Die Demo behauptet Safari 10.3, während das Projekt moderne APIs/CSS wie Constructable Stylesheets, Popover, `color-mix`, Nesting und Scoped Registries nutzt. Mindestversionen pro Feature/Fallback dokumentieren. [`u2/tests/test.html`](u2/tests/test.html#L235)
- [ ] **README-Demolinks auf HTTPS und einen verlässlichen Host umstellen:** Der Generator schreibt `http://gcdn.li/...`; Linkchecker in CI ergänzen. [`u2/tools/update.repos.json.js`](u2/tools/update.repos.json.js#L208)
- [ ] **Dokumentationssprache und Begriffe vereinheitlichen:** Produktionscode/-kommentare mischen Deutsch/Englisch; Katalogtexte enthalten Tippfehler wie `declarativ`, `selektor`, Verzeichnisnamen wie `snippeds` und Toolname `ce-manfest`. Einmalige, bewusst geplante Naming-Bereinigung plus Redirect/Kompatibilitätsentscheidung.
- [ ] **Dead Code und Entwurfsnotizen inventarisieren:** Große auskommentierte Blöcke, `old/`, `beta.css`, `oldies.css`, `debug.js`, `zzz/tmp/x...` und Chat-artige Kommentare erhöhen Wartungskosten. Pro Artefakt behalten+kennzeichnen, in `experiments/` verschieben oder entfernen – nicht blind löschen.
- [ ] **README/Manifest/API-Diff automatisieren:** Observed attributes, Events, CSS Properties/Parts, Slots, Exports und Beispiele aus Tests müssen zueinander passen; `u2-out` zeigt bereits deutliche Drift. [`el/out/manifest.json`](el/out/manifest.json#L1)
- [ ] **Testseiten von externem Netz entkoppeln:** Bilder, Fonts, CDNs und APIs machen lokale Tests langsam/flaky und verstecken echte Fehler hinter Netzproblemen. Lokale Fixtures und explizite Integrationstests trennen.
- [ ] **Root-Smoke-Test bereinigen:** Doppelte IDs (`reposSec`), Tippfehler (`feaures`) und ungültige System-Styler-Farbwerte entfernen; Konsolenwarnungen als Testfehler sammeln statt nur auszugeben. [`u2/tests/test.html`](u2/tests/test.html#L56) [`el/system/styler.js`](el/system/styler.js#L477)
- [ ] **Browser-Test-Infrastruktur dokumentieren:** Ein einfacher lokaler HTTP-Start plus reproduzierbare Browser-Kommandos/Runner gehören ins Repo. Tests dürfen nicht davon abhängen, dass ein Entwickler jede Demo manuell öffnet.

## P3 – API-Konsistenz und Pflege

- [ ] **Lifecycle-Konvention für alle Custom Elements festlegen:** Konstruktor nur für internen Zustand, Setup idempotent in `connectedCallback`, externe Listener/Observer/Timer in `disconnectedCallback` abbauen, Reconnect testen. Besonders Calendar, Breadcrumb, Tooltip, Typewriter, Pagination, Video und Table prüfen.
- [ ] **Fehler-/Ladezustände standardisieren:** Remote-abhängige Elemente sollten konsistent `state=loading|loaded|error`, ein Error-Event, Retry und Fallback-Inhalt anbieten statt gemischter `console.error`/stiller Fehler.
- [ ] **Events vereinheitlichen:** Benennung (`u2-carousel.slide` vs. `u2-*-change`), `detail`, bubbles/composed/cancelable und Reihenfolge dokumentieren; Manifest soll dieselben Events ausweisen.
- [ ] **Attribute/Properties reflektieren:** Boolean-, Number-, Date- und URL-Attribute brauchen einheitliche Parsing-, Default-, Invalid- und Reflection-Regeln. Keine Mischung aus leeren Strings, `+value || default` und frei geworfenen Fehlern.
- [ ] **Mehrinstanz-/Mehrversionstests ergänzen:** Zwei U2-Versionen, mehrere Documents/iframes, mehrere Shadow Roots und Reconnects dürfen keine globalen Cache-/Listener-/Registry-Kollisionen erzeugen.
- [ ] **Security/CSP-Testprofil einführen:** Strikte CSP ohne `unsafe-inline`, Trusted Types, bösartige SVG/Heading/AI-Fixtures, URL-Schema-Tests und Sanitizer-Vertrag automatisieren.
- [ ] **Accessibility-Regressions automatisieren:** Axe/WPT-artige Checks plus echte Tastaturflüsse für Accordion, Tabs, MenuButton, Tree, Carousel, Rating, Splitpanel, Dialog/Tooltip und Form-Elemente; Reduced Motion, Forced Colors, Zoom und RTL einschließen.
- [ ] **Performance-Budgets definieren:** Modul-/CSS-Größe, initiale Requests, Auto-Scan-Zeit, Observer-/Listenerzahl und Scroll/Resize-Framebudget messen und in CI gegen Regressionen schützen.

## Ergänzungen aus dem zweiten Durchgang – P0

- [ ] **Autoformat-URLs ohne `innerHTML` verlinken:** Als Text gelieferte URLs werden ungeescaped zugleich in `href` und Linkinhalt eingesetzt. Ein Wert mit Anführungszeichen kann dadurch Attribute/Markup einschleusen. `URL` validieren und das `a`-Element mit DOM-APIs, `textContent` und `rel=noopener` bauen. [`el/table/ext/autoFormatTable.js`](el/table/ext/autoFormatTable.js#L172)
- [ ] **Attributwerte nie unquoted in Shadow-HTML interpolieren:** `icon` kann bei Accordion, Alert und Rating aus einem korrekt gesetzten Attribut in neues Markup ausbrechen. Elemente per DOM-API erzeugen oder mindestens quoted/escaped setzen. [`el/accordion/accordion.js`](el/accordion/accordion.js#L88) [`el/alert/alert.js`](el/alert/alert.js#L105) [`el/rating/rating.js`](el/rating/rating.js#L82)
- [ ] **Skeleton-DSL begrenzen:** `template` wird nahezu direkt zu beliebigen Tags/verschachteltem `innerHTML`. Grammatik und Tag-Allowlist definieren sowie Zeilen-, Spalten- und Knotenzahl begrenzen, damit untrusted Konfiguration weder Markup einschleust noch riesige DOM-Bäume erzeugt. [`el/skeleton/skeleton.js`](el/skeleton/skeleton.js#L30)
- [ ] **Dialog-HTML-Vertrauensgrenze explizit machen:** Objekt-`body`, Button-Titel und Modaloptionen gehen direkt in `innerHTML`. Eine sichere Text/Node-API als Default anbieten und bewusstes Trusted-HTML separat kennzeichnen. [`js/dialog/dialog.js`](js/dialog/dialog.js#L20)

## Ergänzungen aus dem zweiten Durchgang – P1 Kernhelfer

- [ ] **SelectorObserver-Roots freigeben:** Die globale `Map` hält jedes Document/ShadowRoot und dessen MutationObserver dauerhaft. Referenzzählung pro Root einführen und den Root-Observer trennen/löschen, sobald dort kein SelectorObserver mehr aktiv ist. [`js/SelectorObserver/SelectorObserver.js`](js/SelectorObserver/SelectorObserver.js#L1)
- [ ] **`disconnect()` muss `off` ausführen:** Aktuell wird `targets` nur geleert; von `on` installierte Listener/Observer bleiben aktiv. Vor dem Leeren jedes aktive Ziel sauber abmelden und mehrfaches `observe()`/Root-Wechsel definieren. [`js/SelectorObserver/SelectorObserver.js`](js/SelectorObserver/SelectorObserver.js#L90)
- [ ] **Frühe Subtree-Erkennung nicht vom Ready-State abhängig machen:** `_addTree()` scannt Nachfahren nur bei `document.readyState === 'complete'`; dynamisch eingefügte Teilbäume während `loading`/`interactive` bleiben teilweise unerkannt. [`js/SelectorObserver/SelectorObserver.js`](js/SelectorObserver/SelectorObserver.js#L141)
- [ ] **PointerObserver entsorgbar machen:** Die permanenten `mousedown`/`touchstart`-Listener am Zielelement haben keinen `disconnect()`-Pfad. Startlistener und laufenden AbortController kontrolliert abbauen. [`js/PointerObserver/PointerObserver.js`](js/PointerObserver/PointerObserver.js#L24)
- [ ] **Pointer-Abbruch vollständig behandeln:** `touchcancel`, verlorener Pointer, Window-Blur und Elemententfernung können `running` hängen lassen. Auf Pointer Events plus Capture umstellen oder alle Abbruchpfade explizit behandeln. [`js/PointerObserver/PointerObserver.js`](js/PointerObserver/PointerObserver.js#L68)
- [ ] **Pointer-Zeitdifferenzen initialisieren:** `posStart` und die erste `pos` enthalten keinen Zeitwert; `diff.time`/`startDiff.time` sind bis nach einer Bewegung `NaN`. Einen konsistenten Snapshot mit Timestamp verwenden. [`js/PointerObserver/PointerObserver.js`](js/PointerObserver/PointerObserver.js#L59)
- [ ] **Placer-Updates coalescen statt pollen:** `followElement()` startet zusätzlich zu fünf globalen Listenern ein 200-ms-Interval; viele Menüs/Tooltips skalieren schlecht. ResizeObserver/Scroll-Scheduler verwenden und pro Frame höchstens einmal messen/schreiben. [`js/Placer/Placer.js`](js/Placer/Placer.js#L117)
- [ ] **Absolute Koordinaten relativ zum echten Containing Block berechnen:** Globale `scrollX/scrollY` sind für gescrollte/transformierte Offset-Parents falsch. Scrolloffset, Border und Writing Mode des tatsächlichen Containers berücksichtigen. [`js/Placer/Placer.js`](js/Placer/Placer.js#L48)
- [ ] **Veraltete Placer-RAFs verhindern:** Jede Positionierung plant einen ungespeicherten Frame; ältere Berechnungen können nach neueren schreiben. Pending Frame ersetzen/abbrechen und beim `unfollow()` entfernen. [`js/Placer/Placer.js`](js/Placer/Placer.js#L108)
- [ ] **Loading-Ziele typgerecht erkennen:** `if (!el.length)` verwechselt Elemente mit `length` (z. B. Select/Form) und behandelt eine leere NodeList als einzelnes Element. Explizit `Node` versus Iterable unterscheiden. [`js/loading/loading.js`](js/loading/loading.js#L2)
- [ ] **Loading nicht durch beliebige Child-Mutationen beenden:** Der interne MutationObserver ruft bei jeder direkten DOM-Änderung `done()` auf und kann mehrere unabhängige Tasks vorzeitig herunterzählen. Abschluss ausschließlich über die zurückgegebene Funktion bzw. klar definierte Quellen steuern. [`js/loading/loading.js`](js/loading/loading.js#L20)
- [ ] **Loading-Counter gegen doppelte Abschlüsse schützen:** Zweimaliges Aufrufen einer Done-Funktion macht den Zähler negativ und kann fremde Tasks beenden. Pro Markierung ein idempotentes Token zurückgeben. [`js/loading/loading.js`](js/loading/loading.js#L8)
- [ ] **Inline-Styles beim Loading nicht komplett zurückrollen:** Das Speichern/Wiederherstellen von `style.cssText` löscht legitime Änderungen, die während des Ladens entstanden sind. Nur selbst gesetzte Properties samt vorherigem Einzelwert verwalten. [`js/loading/loading.js`](js/loading/loading.js#L44)

## Ergänzungen aus dem zweiten Durchgang – P1 Attribute

- [ ] **Behavior-Listener im `off`-Pfad entfernen:** Attribut entfernen/erneut setzen akkumuliert Click-, Scroll- und globale Listener, weil `off` leer ist. Jedes `init` soll einen Disposer liefern. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L3)
- [ ] **Darkmode ohne `undefined`-Attribute initialisieren:** Ohne Storage-Eintrag entstehen `u2-skin="undefined"` und `data-theme="undefined"`. Systempräferenz/Default verwenden oder Attribute entfernen; StorageError abfangen. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L187)
- [ ] **`once` braucht einen stabilen eindeutigen Key:** Alle Elemente ohne ID teilen derzeit `u2-behavior-once-` und verstecken sich gegenseitig. Fehlende/duplizierte IDs ablehnen oder expliziten Storage-Key verlangen. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L112)
- [ ] **Behavior-Texte wirklich lokalisieren:** Trotz vorhandener Sprachmaps wird immer `.en` eingesetzt. Nächstes `lang`, Locale-Normalisierung und Text-vs.-HTML-Vertrag verwenden. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L22)
- [ ] **Install-Prompt nur einmal und entsorgbar binden:** Jedes Element registriert global `beforeinstallprompt` und pro Event nochmals Click-Listener; Reconnects und mehrere Buttons prompten mehrfach. Den Deferred Prompt zentral verwalten und nach Verwendung/`appinstalled` löschen. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L139)
- [ ] **Fullscreen-Zustand vom Dokumentereignis synchronisieren:** Externe Vollbildwechsel/Escape aktualisieren Behavior-State nicht; außerdem kann `parentElement` fehlen. `fullscreenchange` beobachten und Ziel explizit konfigurierbar machen. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L163)
- [ ] **Wake-Lock-Steuerungen koordinieren:** Ein Modul-Global wird von mehreren Buttons geteilt, aber das Release-Event aktualisiert nur den ursprünglich schließenden Button. Gemeinsamen zustandsbehafteten Controller mit Subscriptions oder Instanzzustand verwenden. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L232)
- [ ] **Behavior-Aktionen mit `finally` entsperren und Fehler melden:** Fehler werden vollständig verschluckt; bei unerwarteten Fehlern vor dem letzten Statement kann `inert` hängen. `try/catch/finally`, Error-Event und definierte Success-Dauer ergänzen. [`attr/behavior/behavior.js`](attr/behavior/behavior.js#L28)
- [ ] **Confirm-Reentrancy pro Event/Element lösen:** Das Modul-global geteilte `resentConfirmed` und parallel offene Dialoge können fremde Bestätigungen beeinflussen. Pending-Vorgänge tokenisieren und genau das ursprüngliche Event kontrolliert fortsetzen. [`attr/confirm/confirm.js`](attr/confirm/confirm.js#L1)
- [ ] **Confirm-Importfehler nicht als stillen Klickverlust enden lassen:** Der ursprüngliche Event wird sofort gestoppt; schlägt der dynamische Dialogimport fehl, passiert nichts mehr. Fehlerereignis/Fallback und einen dokumentierten Replay-Vertrag vorsehen. [`attr/confirm/confirm.js`](attr/confirm/confirm.js#L8)
- [ ] **Confirm-Locale normalisieren:** `lang="de-CH"` findet den Schlüssel `de` nicht und fällt unnötig auf Englisch zurück. BCP-47-Basissprache plus Fallbackkette verwenden. [`attr/confirm/confirm.js`](attr/confirm/confirm.js#L46)
- [ ] **Dropzone-Strategie vor der ersten Bewegung vorbereiten:** Beim Dragstart wird nur `currentStrategy` gesetzt. Bleibt die erste gültige Zone bei derselben Strategie, ruft Dragover nie `prepare()` auf; Move kann nicht zurückrollen und Ghost erzeugt keinen Clone. [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L14)
- [ ] **Dropzone-Zustand nach Drag immer zurücksetzen:** `currentStrategy`, `currentDropzone` und `currentBefore` bleiben absichtlich stehen. Der nächste Drag kann deshalb das erste `move()` überspringen und alte Referenzen verwenden. [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L29)
- [ ] **Strategien pro Drag instanziieren:** `originalParent`, `originalNext` und `clone` liegen auf globalen Singleton-Objekten. Überlappende/abgebrochene Drags und mehrere Documents teilen veränderlichen Zustand. [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L136)
- [ ] **Dropzone-Selektoren ohne temporäre ID bauen:** `u2-dropzone-tmp` kann mit vorhandenen IDs kollidieren; echte IDs werden nicht mit `CSS.escape()` geschützt. `:scope` über root-lokales Matching oder garantiert eindeutige escaped IDs behandeln, ohne Nutzer-DOM zu mutieren. [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L313)
- [ ] **Dropzones in Shadow Roots inventarisieren:** Eventauflösung nutzt zwar `composedPath()`, Valid-Markierung und Cleanup durchsuchen aber nur `document`. Aktive Root-Bäume sammeln oder ausschließlich eventlokal arbeiten. [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L14)
- [ ] **Lightbox-Selector escapen:** Der frei wählbare Gruppenname wird direkt in `a[u2-lightbox="…"]` interpoliert; Quotes/Sonderzeichen werfen `SyntaxError`. `CSS.escape()` oder Attributvergleich ohne Stringselektor verwenden. [`attr/lightbox/lightbox.js`](attr/lightbox/lightbox.js#L68)
- [ ] **Lightbox erst bei vorhandenem Body mounten:** Das Overlay wird bei Modulevaluation sofort an `document.body` gehängt und kann bei frühem Import crashen. Lazy beim ersten Gebrauch oder nach DOM-Ready erzeugen. [`attr/lightbox/lightbox.js`](attr/lightbox/lightbox.js#L4)
- [ ] **Lightbox-Carousellistener deduplizieren:** Jeder Klick registriert einen weiteren `u2-carousel.slide`-Listener am selben Carousel. Einmal binden/abbauen und den auslösenden Link nach Close wieder fokussieren. [`attr/lightbox/lightbox.js`](attr/lightbox/lightbox.js#L95)
- [ ] **Movable darf vorhandene Transforms nicht überschreiben:** Die Transform-Strategie schreibt ausschließlich `translate(...)` und verliert Rotation, Scale oder Skew. Translation composable über individuelle Transform-Properties/Matrix erhalten. [`attr/movable/movable.js`](attr/movable/movable.js#L4)
- [ ] **Movable-Touchstart nicht passiv verdrahten, wenn Start preventet:** Der Dokumentlistener ist passiv, während der Startcallback `preventDefault()` erwartet; Scrollunterdrückung funktioniert dadurch nicht zuverlässig. Pointer Events und `touch-action` gezielt einsetzen. [`attr/movable/movable.js`](attr/movable/movable.js#L60)
- [ ] **TargetObserver-Elternprüfung null-/typfest machen:** Bei `in` kann `closest().parentNode` ein Document sein und besitzt kein `closest()`. Außerdem muss ein fehlender `matches`-Selektor definiert werden. [`attr/navigable/TargetObserver.js`](attr/navigable/TargetObserver.js#L41)
- [ ] **Hash-IDs decodieren:** Rohe percent-encoded Fragmente finden Ziele mit Leerzeichen/Unicode nicht; fehlerhafte Encodings dürfen zugleich nicht werfen. [`attr/navigable/TargetObserver.js`](attr/navigable/TargetObserver.js#L70)
- [ ] **URL-Target-Set sauber serialisieren:** Der leere Parameter erzeugt zunächst ein Set mit `""`, dadurch führende Leerzeichen; beim Entfernen des letzten Ziels bleibt `?u2-target=` stehen. Leere Tokens filtern und Parameter vollständig löschen. [`attr/navigable/U2TargetObserver.js`](attr/navigable/U2TargetObserver.js#L64)
- [ ] **Bestehenden History-State bewahren:** Die Hash-Konvertierung ruft `replaceState(null, ...)` auf und löscht z. B. den Navigator-State. Vorhandenen State durchreichen/mergen. [`attr/navigable/navigable.js`](attr/navigable/navigable.js#L6)
- [ ] **Scrollspy-Auswahl deterministisch machen:** Der erste Eintrag eines Sets hängt von IntersectionObserver-Callbackreihenfolge ab, nicht zuverlässig von Dokument-/Viewportreihenfolge. Sichtbare Kandidaten geometrisch sortieren. [`attr/scrollspy/scrollspy.js`](attr/scrollspy/scrollspy.js#L38)
- [ ] **Scrollspy auf Link-/ID-Änderungen reagieren:** Beobachtet wird nur `childList`; geänderte `href`/`id` und mehrere Links auf dasselbe Ziel bleiben stale bzw. werden im Map überschrieben. [`attr/scrollspy/scrollspy.js`](attr/scrollspy/scrollspy.js#L54)
- [ ] **Selectable dynamische Items initialisieren:** Nachträglich eingefügte Kinder bekommen weder Rollen/ARIA noch die Manager-Semantik. MutationObserver oder explizite `refresh()`-API ergänzen. [`attr/selectable/selectable.js`](attr/selectable/selectable.js#L6)
- [ ] **SelectionManager leere Listen absichern und Änderungen emittieren:** Keyboardpfade können `focus(undefined)` erreichen; Auswahl ändert nur ARIA, ohne `input`/`change`/Custom Event. [`attr/selectable/SelectionManager.js`](attr/selectable/SelectionManager.js#L65) [`attr/selectable/SelectionManager.js`](attr/selectable/SelectionManager.js#L136)
- [ ] **Intersect-Observer wirklich disconnecten:** `off` unobserved nur das Element, lässt Observer und Property-Zyklus bestehen. `disconnect()`, Property löschen und Re-add testen. [`attr/intersect/intersect.js`](attr/intersect/intersect.js#L44)
- [ ] **Intersect-Konfiguration validieren und Events nur bei Zustandswechsel senden:** Threshold außerhalb 0..1 oder ungültiger Root-Margin lässt den Konstruktor werfen; Enter/Leave wird bei jedem Callback erneut emittiert. [`attr/intersect/intersect.js`](attr/intersect/intersect.js#L8)
- [ ] **Draghandle ursprüngliches `draggable` wiederherstellen:** Beim Verlassen wird dauerhaft `draggable="false"` geschrieben, auch wenn das Attribut vorher fehlte oder true war. Originalzustand pro Element merken; globalen Pointermove-Hotpath drosseln. [`attr/draghandle/draghandle.js`](attr/draghandle/draghandle.js#L16)

## Ergänzungen aus dem zweiten Durchgang – P1 Elemente

- [ ] **Bytes/Number echte Textänderungen beobachten:** `characterData:true` ohne `subtree:true` sieht Änderungen am enthaltenen Textknoten nicht. Gleichzeitig Parsing/Intl-Optionen validieren, damit ungültige Unit/Currency/Digits nicht den Renderpfad werfen. [`el/bytes/bytes.js`](el/bytes/bytes.js#L7) [`el/number/number.js`](el/number/number.js#L7)
- [ ] **Number-Parsing nicht mit `parseFloat` beschönigen:** Werte wie `12foo` werden als 12 formatiert. Vollständiges Format akzeptieren oder einen sichtbaren Invalid-State beibehalten. [`el/number/number.js`](el/number/number.js#L17)
- [ ] **Time ohne `datetime` stabil machen:** `reset()` ruft `.match()` auf `null` auf; ein leeres Element oder eine andere Attributänderung vor `datetime` crasht. Fehlenden/ungültigen Wert explizit rendern. [`el/time/time.js`](el/time/time.js#L22)
- [ ] **Relative Time-Timer für vergangene Werte korrigieren:** Negative Modulo-Werte berechnen den nächsten Tick zu spät. Zeit bis zur nächsten tatsächlichen Einheitengrenze positiv normalisieren. [`el/time/time.js`](el/time/time.js#L137)
- [ ] **Time-Optionen vollständig beobachten:** `timeZoneName` steht in Defaults, aber nicht in `observedAttributes`; CamelCase-Attributnamen sind zudem HTML-untypisch. README, Property- und Attribut-API synchronisieren. [`el/time/time.js`](el/time/time.js#L150)
- [ ] **Copy-Target-Rennen abbrechen:** Bei schnellem Wechsel `for=A` zu `for=B` kann das später auftauchende A den bereits aktuellen B-Clone überschreiben. Sequenz/AbortSignal verwenden. [`el/copy/copy.js`](el/copy/copy.js#L14)
- [ ] **Endloses ID-Polling ersetzen:** `promiseElementById()` plant für eine nie erscheinende ID unbegrenzt Animation Frames. Timeout/AbortSignal, MutationObserver und Root-Scope anbieten. [`u2/tools/promiseElementById.js`](u2/tools/promiseElementById.js#L2)
- [ ] **Copy-IDREFs vollständig umschreiben:** Beim Clone wird nur `for` angepasst; `aria-*`, `headers`, `list`, Fragmentlinks und SVG-Referenzen zeigen weiterhin auf Original-IDs. Einen zentralen ID/IDREF-Rewriter verwenden. [`el/copy/copy.js`](el/copy/copy.js#L57)
- [ ] **Skeleton-Renderframes versionieren:** Schnelle Attributänderungen stellen mehrere RAFs ein; ein älterer Callback kann neueren Zustand überschreiben. Nur den letzten Render ausführen. [`el/skeleton/skeleton.js`](el/skeleton/skeleton.js#L10)
- [ ] **Counter mit einem RAF statt 15-ms-Interval animieren:** Der Interval kann schneller feuern als Paint und redundante Frames aufstauen. Zeitbasierten Einzel-RAF plus Reduced-Motion-Pfad verwenden. [`el/counter/counter.js`](el/counter/counter.js#L52)
- [ ] **Counter-NaN und Layoutänderungen behandeln:** Fehlende/ungültige Werte erzeugen `NaN`; die einmal gemessene Endbreite wird bei Font-, Resize- oder Wertänderungen nie aktualisiert. [`el/counter/counter.js`](el/counter/counter.js#L19)
- [ ] **Accordion-Rebuild darf Listener nicht vervielfachen:** `_build()` hängt an vorhandene Trigger jedes Mal einen neuen Mousedown-Listener. AbortController/gebundene Handler verwenden und Attributänderungen (`single`, `icon`) beobachten. [`el/accordion/accordion.js`](el/accordion/accordion.js#L125)
- [ ] **Accordion-Eventziel robust aus `composedPath()` finden:** Ein fixer Offset `rootIndex - 3` bricht bei verschachteltem SVG, Slots oder geändertem Markup. Den ersten bekannten Trigger im Pfad suchen. [`el/accordion/accordion.js`](el/accordion/accordion.js#L141)
- [ ] **Alert-Schreibweise vereinheitlichen:** CSS prüft `dismissable`, Demos/API verwenden `dismissible`; dadurch kann der Close-Button trotz gewünschter Dismiss-Funktion verborgen sein. Manifest, README und Kompatibilitätsalias abstimmen. [`el/alert/alert.js`](el/alert/alert.js#L3)
- [ ] **Alert-Toast-Promises nicht überschreiben:** Zweimaliges `toast()` auf derselben Instanz ersetzt `this.resolve`; das erste Promise bleibt ewig pending. Pro Anzeige eigener Abschluss oder zweite Anzeige ablehnen. [`el/alert/alert.js`](el/alert/alert.js#L119)
- [ ] **Leeren Alert-Stack schließen:** Der globale Popover-Container bleibt nach dem letzten Toast offen/im DOM. Beim Entfernen Zustand und Fokus sauber zurücksetzen. [`el/alert/alert.js`](el/alert/alert.js#L180)
- [ ] **Buttongroup-Abhängigkeiten standalone laden:** Das Modul erzeugt `u2-menubutton` und `u2-ico`, importiert aber nur Focusgroup. README-Standalone muss ohne zufälligen Auto-Loader funktionieren. [`el/buttongroup/buttongroup.js`](el/buttongroup/buttongroup.js#L5)
- [ ] **Buttongroup-ResizeObserver am echten Parent abbauen:** Nach Entfernung ist `parentElement` null; `unobserve(null)` ist ungültig und der alte Parent bleibt beobachtet. Referenz speichern oder `disconnect()` verwenden. [`el/buttongroup/buttongroup.js`](el/buttongroup/buttongroup.js#L59)
- [ ] **Menubutton ohne Menu und vor Init fehlertolerant machen:** Der verzögerte Init dereferenziert ein fehlendes `<menu>`; `open/close/toggle` können vor Ablauf des Timers ebenfalls auf undefined zugreifen. Init synchron/idempotent und dynamische Menüs beobachtbar machen. [`el/menubutton/menubutton.js`](el/menubutton/menubutton.js#L3)
- [ ] **Drawer-Reconnect idempotent machen:** Swipe-Listener werden pro Verbindung erneut angehängt; im contained-Modus versucht `attachShadow()` ein zweites Mal. Einmaliges Setup plus echter Teardown. [`el/drawer/drawer.js`](el/drawer/drawer.js#L12)
- [ ] **Drawer-Swipezustand bei Touchstart resetten:** `currentX/currentY` bleiben vom letzten Swipe erhalten; ein späterer Tap kann mit altem Delta schließen. Placement validieren und Touchcancel behandeln. [`el/drawer/drawer.js`](el/drawer/drawer.js#L104)
- [ ] **Drawer-contained Transformkonflikt lösen:** CSS erzwingt am Host `transform:none !important`, während Swipe genau dort Transform schreibt; visuelles Dragging kann nicht funktionieren. Eine innere, einheitliche Bewegungsebene verwenden. [`el/drawer/drawer.js`](el/drawer/drawer.js#L37)
- [ ] **Carousel-Nav ohne Legacy-Prototyperweiterung betreiben:** Sync-Gruppen rufen `document.body.c1FindAll()` auf, das im standalone Modul nicht existiert. Standardselektoren/öffentliche Carousel-API verwenden. [`el/carousel-nav/carousel-nav.js`](el/carousel-nav/carousel-nav.js#L34)
- [ ] **Carousel-Nav fehlendes Carousel und Reconnect behandeln:** Ohne verwandtes Carousel crasht `_items()`; Reconnect dupliziert Buttons/Listener. Verknüpfung validieren, teardownen und dynamische Slides beobachten. [`el/carousel-nav/carousel-nav.js`](el/carousel-nav/carousel-nav.js#L43)
- [ ] **Carousel-Nav als echte Controls ausgeben:** Nicht fokussierbare Spans brauchen Buttonsemantik, Namen, Keyboard und `aria-current`; nicht auf private `_items()` zugreifen. [`el/carousel-nav/carousel-nav.js`](el/carousel-nav/carousel-nav.js#L52)
- [ ] **Fields-Labels korrekt mit Controls assoziieren:** Ein Shadow-Label kann ein geslottetes Light-DOM-Control nicht verlässlich durch Wrapping beschriften; der Kommentar markiert den aktuellen ElementInternals-Ansatz selbst als wirkungslos. Explizite IDs/ARIA außerhalb der Shadow-Grenze verwenden. [`el/fields/fields.js`](el/fields/fields.js#L86)
- [ ] **Fields keine Labels verschachteln oder Restinhalt verlieren:** Vorhandene `<label>` werden als Inhalt in ein neues Label gelegt; Inhalt nach dem letzten Input wird verworfen. Parser als verlustfreie Node-Transformation auslegen. [`el/fields/fields.js`](el/fields/fields.js#L62)
- [ ] **Fields-Werte an FormData-Semantik angleichen:** `Object.fromEntries`-ähnliches Überschreiben verliert Checkboxgruppen/duplizierte Namen. `FormData` oder Arrays für Mehrfachwerte nutzen. [`el/fields/fields.js`](el/fields/fields.js#L107)
- [ ] **Masonry alle Kinderobserver abbauen:** Disconnect beobachtet nur den Host ab; entfernte und aktuelle Children bleiben im ResizeObserver und können gehalten werden. `disconnect()` nutzen und Inline-Layoutwerte restaurieren. [`el/masonry/masonry.js`](el/masonry/masonry.js#L7)
- [ ] **Masonry-Renders coalescen und leeren Zustand korrigieren:** Jeder Observercallback plant einen RAF; eigene Writes können weitere Layoutzyklen auslösen. Bei ausschließlich versteckten Kindern wird die Höhe außerdem `-rowGap`. [`el/masonry/masonry.js`](el/masonry/masonry.js#L27)
- [ ] **Spot bei Resize tatsächlich neu positionieren:** `updatePosition()` ruft `moveTo(currentTarget)`, aber `moveTo()` kehrt bei demselben Target sofort zurück. Reposition und Targetwechsel trennen. [`el/spot/spot.js`](el/spot/spot.js#L34)
- [ ] **Spot-Selektoren und Anchor-Zustand sicher verwalten:** Konkatenierte freie Selektoren können werfen; zufälliges `anchor-name` wird dauerhaft am Ziel hinterlassen und kann kollidieren. Selektoren getrennt matchen und Änderungen rückgängig machen. [`el/spot/spot.js`](el/spot/spot.js#L20)
- [ ] **Tree-Navigation zum tiefsten sichtbaren Vorgänger:** `prevFocusable()` steigt beim vorherigen expandierten Geschwister nur eine Ebene hinab. Rekursiv bis zum letzten sichtbaren Nachfahren gehen. [`el/tree/tree.js`](el/tree/tree.js#L156)
- [ ] **Tree-Lazyload gegen Sync-Callbacks, Fehler und Mehrfachlistener härten:** `event.load(...).then(...)` setzt zwingend ein Promise voraus; Throws/Rejections und mehrere Loader lassen Zustand hängen. Einen klaren cancelbaren Load-Vertrag definieren. [`el/tree/tree.js`](el/tree/tree.js#L168)
- [ ] **Tree-State bei entfernten Nodes reparieren:** Root hält `active`/`selected` auf nicht mehr verbundenen Elementen; Roving-Tabindex wird bei Mutationen nicht neu hergestellt. [`el/tree/tree.js`](el/tree/tree.js#L221)
- [ ] **Tree-Item-Schlüssel in Selektoren escapen und Listener entsorgen:** `data-key` wird ungeescaped interpoliert, Item-Change-Listener werden nie entfernt; Zyklen, Duplikate und Reordering sind undefiniert. [`el/tree/ext/item.js`](el/tree/ext/item.js#L5)
- [ ] **Table vor einem vorhandenen `<table>` nicht crashen lassen:** Mutationcallback ruft `this.columns.refresh()` ohne Table-Guard auf. Wird die Tabelle erst später eingefügt, ist `columns` undefined. [`el/table/table.js`](el/table/table.js#L15)
- [ ] **Table-Austausch vollständig reinitialisieren:** Interne Flags und Columns zeigen nach Austausch auf die alte Tabelle; Checkbox-/Drag-Features werden am neuen Element übersprungen. Tabellenidentität beobachten und Instanzzustand resetten. [`el/table/table.js`](el/table/table.js#L31)
- [ ] **Table-Checkboxen bei null/leer und programmatischen Änderungen synchronisieren:** Null-Header/keine Rows können crashen bzw. `allChecked=true` ergeben; Summary-Toggle sendet keine Input/Change-Events. [`el/table/table.js`](el/table/table.js#L81)
- [ ] **Table-Aggregate nur aus gültigen Zahlen bilden:** `NaN` vergiftet Summen, Average zählt leere/nichtnumerische Zellen mit und Text-/Inputänderungen werden wegen fehlendem `characterData` nicht beobachtet. [`el/table/table.js`](el/table/table.js#L134)
- [ ] **Autoformat-Rennen und Destruktivität beseitigen:** Async-Import/Format kann nach Table-Austausch laufen und `_isUpdating` bei Fehlern hängen lassen; Number/Date/Boolean-Formatierung ersetzt Originalinhalt irreversibel. [`el/table/table.js`](el/table/table.js#L126) [`el/table/ext/autoFormatTable.js`](el/table/ext/autoFormatTable.js#L80)
- [ ] **Autoformat locale-/engine-stabil machen:** `Date.parse`-Heuristik und `!isNaN` klassifizieren Werte browserabhängig/zu großzügig; Datumsformat ist hart `de-CH`. Strikte Parser anhand `lang`/Spaltenschema verwenden und alte Auto-Klassen vor Re-run entfernen. [`el/table/ext/autoFormatTable.js`](el/table/ext/autoFormatTable.js#L80)
- [ ] **Tabs gleiche Anzahl Tabs/Panels validieren:** `_selectTab()` dereferenziert `this.panels[i]` für jeden Tab; ein fehlendes Panel crasht das gesamte Element. Ungleiche Mengen und dynamische Slotchanges definieren. [`el/tabs/tabs.js`](el/tabs/tabs.js#L86)
- [ ] **Tabs-Klicks aus verschachteltem Inhalt erkennen:** Nur `event.target.slot === 'title'` gilt; Klick auf Icon/Span innerhalb eines Tabs wird ignoriert. `composedPath()`/`closest` zum zugewiesenen Tab verwenden. [`el/tabs/tabs.js`](el/tabs/tabs.js#L110)
- [ ] **Tabs-Auswahl clampen und ARIA-Beziehungen ergänzen:** Negative, NaN- oder zu große `selected`-Werte verstecken alle Panels; `aria-controls`/`aria-labelledby`, Home/End, Wrap und RTL fehlen. [`el/tabs/tabs.js`](el/tabs/tabs.js#L83)
- [ ] **Rating ohne Startwert tastaturfähig machen:** `parseFloat(null)` setzt intern `NaN`; Arrow-Key-Rechnung bleibt danach NaN. Einen gültigen leeren/0-Zustand modellieren und auf 1..max clampen. [`el/rating/rating.js`](el/rating/rating.js#L95)
- [ ] **Rating vollständig form-associated implementieren:** `required` wird nicht beobachtet, Form-Reset/State-Restore/Disabled fehlen und Nutzereingaben emittieren kein `input`/`change`. [`el/rating/rating.js`](el/rating/rating.js#L27)
- [ ] **Rating-Max als positive ganze Zahl validieren:** Bruchwerte erzeugen mehr Icons als `ariaMax` behauptet; negative/Infinity-Werte brauchen einen Fallback. Arrow Keys müssen Defaultscroll verhindern und RTL beachten. [`el/rating/rating.js`](el/rating/rating.js#L82)
- [ ] **Video-Reconnect ohne neue Media-Instanz/Listener:** Intern erzeugte Videos liegen im neu gerenderten Shadow DOM und werden beim Reconnect ersetzt; Light-DOM-Videos sammeln doppelte Listener. Einmaliges Rendering, stabile Video-Referenz und AbortController verwenden. [`el/video/video.js`](el/video/video.js#L18)
- [ ] **Video beobachtete Attribute wirklich spiegeln:** `controls`, `width`, `height` und `picture-in-picture` werden beobachtet, aber im Callback ignoriert; entfernte `src/poster/preload/crossorigin` werden als `null` gesetzt. Hinzufügen/Entfernen symmetrisch behandeln. [`el/video/video.js`](el/video/video.js#L12) [`el/video/video.js`](el/video/video.js#L441)
- [ ] **Video-Promises/Mediawerte absichern:** `play()`-Rejection wird ignoriert, numerische Shortcuts können bei unbekannter Duration `currentTime=NaN` setzen und Progress rendert `NaN%`. UI nur nach Erfolg aktualisieren und finite Werte clampen. [`el/video/video.js`](el/video/video.js#L363)
- [ ] **Video-API vor Connection und Eventweitergabe definieren:** Gespiegelte HTMLVideoElement-Methoden dereferenzieren vor Setup `null`; Events eines intern im Shadow DOM erzeugten Videos erreichen Host-Consumer nicht automatisch. Ready-Vertrag und Event-Forwarding ergänzen. [`el/video/video.js`](el/video/video.js#L468)
- [ ] **Cookiebanner-Scriptattribute beim Freischalten erhalten:** Der Ersatz kopiert nur `src` oder Text und erzwingt `text/javascript`; `type=module`, nonce, integrity, crossorigin, referrerpolicy, async/defer und weitere Semantik gehen verloren. Erlaubte Attribute vollständig/sicher übertragen. [`el/cookiebanner/cookiebanner.js`](el/cookiebanner/cookiebanner.js#L83)
- [ ] **Cookie-/Storage-Wildcards tatsächlich anwenden:** `knownItems.js` enthält viele Keys wie `_ga_*`, aber Cleanup prüft nur exakte Namen. Vorhandene Keys/Cookies gegen sichere Patterns matchen und Löschung für Domain/Path realistisch dokumentieren. [`el/cookiebanner/cookiebanner.js`](el/cookiebanner/cookiebanner.js#L107) [`el/cookiebanner/knownItems.js`](el/cookiebanner/knownItems.js#L26)
- [ ] **Consent-Cookie robust kodieren, versionieren und validieren:** Rohes JSON wird ohne Encoding/Schema geschrieben; `Secure` verhindert Persistenz auf nicht-HTTPS-Entwicklungssystemen und beliebige alte/kaputte Shapes werden akzeptiert. Version/Migration, `encodeURIComponent`, Max-Age und Storagefehler behandeln. [`el/cookiebanner/cookiebanner.js`](el/cookiebanner/cookiebanner.js#L45)
- [ ] **Consent-Entzug nicht als vollständiges Tracking-Cleanup versprechen:** Bereits ausgeführte Scripts, Requests und in-memory Tracker werden durch Cookie/Storage-Löschung nicht gestoppt. Integrationen brauchen explizite Disable-/Reload-Hooks und eine klare Compliance-Grenze. [`el/cookiebanner/cookiebanner.js`](el/cookiebanner/cookiebanner.js#L107)
- [ ] **Cookiebanner-Mehrinstanzzustand entfernen:** `initialEmitted` ist modulglobal und jede Instanz erzwingt dieselbe ID; nur die erste Instanz meldet initialen Consent und mehrere Banner erzeugen ID-Kollisionen. Singleton explizit erzwingen oder Zustand pro Instanz/Root führen. [`el/cookiebanner/cookiebanner.js`](el/cookiebanner/cookiebanner.js#L4)
- [ ] **Policy-Link auf erlaubte URLs beschränken:** HTML-Escaping verhindert kein gefährliches URL-Schema. URL parsen, relative/http(s)-Ziele erlauben und bei `target=_blank` `rel=noopener` setzen. [`el/cookiebanner/ui.js`](el/cookiebanner/ui.js#L16)
- [ ] **Code-Element Reconnect und Fremdquelle sauber abbauen:** `_init()` hängt Input/Blur bei jedem Connect erneut an; der MutationObserver läuft nach Disconnect weiter. Einmaliges Setup plus `disconnect()`/AbortController. [`el/code/code.js`](el/code/code.js#L79)
- [ ] **Code-Element fehlendes/entferntes `element` nullsicher behandeln:** `observe(null)` und anschließendes `getSourceValue()` werfen. Pending-ID-Auflösung oder sichtbaren Fehlerzustand anbieten; Attributentfernung zurück auf lokale Quelle führen. [`el/code/code.js`](el/code/code.js#L156)
- [ ] **Code-Highlight-Rennen und Fehler behandeln:** Mehrere Werte planen unversionierte Promise-/RAF-Callbacks, sodass ein alter Highlight zuletzt rendern kann; unbekannte Sprache oder CDN-Fehler bleiben unhandled. Sequenz, Fallback und Retry ergänzen. [`el/code/code.js`](el/code/code.js#L119)
- [ ] **Live-HTML-Editing als gefährlichen Opt-in kennzeichnen:** Bei fremden Elementen schreibt die Textarea ihren Wert direkt in `innerHTML`; eingegebene aktive Inhalte laufen im Seitendokument. Standardmäßig Text/Preview-Sandbox verwenden oder die Ausführung unmissverständlich separat aktivieren. [`el/code/code.js`](el/code/code.js#L133)
- [ ] **Chart ohne Daten nicht schon beim Destructuring crashen lassen:** `_extractdata()` kann `undefined` liefern, aber `_build()` destructured davor und erreicht den vorgesehenen `if (!data)`-Guard nie. Ergebnis erst prüfen und einen leeren Zustand rendern. [`el/chart/chart.js`](el/chart/chart.js#L52)
- [ ] **Chart-Builds versionieren/abbrechen:** Schnelle Attribut- oder `for`-Wechsel lassen mehrere Imports/Renders parallel laufen; ein älterer Build kann den neuen überschreiben oder nach Disconnect rendern. Sequenz/AbortSignal und finalen Connection-Check verwenden. [`el/chart/chart.js`](el/chart/chart.js#L44)
- [ ] **Chart-Engine allowlisten:** Das freie `engine`-Attribut wird Teil eines dynamischen Importpfads. Nur im Manifest registrierte Adapter laden und unbekannte Namen ohne Pfadnavigation ablehnen. [`el/chart/chart.js`](el/chart/chart.js#L108)
- [ ] **Chart-Datenextraktion für leere/irreguläre Tabellen absichern:** `trs[0].firstElementChild` und `data[0]` setzen Rows/Cells voraus; leere Tabellen, Colspans und ragged Arrays crashen oder werden falsch gedreht. [`el/chart/chart.js`](el/chart/chart.js#L130)
- [ ] **Chart auf Quelldatenänderungen reagieren:** Nach dem ersten Build werden Änderungen an verknüpfter Tabelle/DL nicht beobachtet. Explizite `refresh()`-API oder begrenzten MutationObserver mit Teardown anbieten. [`el/chart/chart.js`](el/chart/chart.js#L37)
- [ ] **Carousel-Scrollmodus-Listener nicht stapeln:** Jeder Wechsel zu `mode=scroll` hängt einen weiteren anonymen Scrolllistener an; spätere Modi entfernen ihn nicht. Modus-Handler brauchen symmetrisches `init/destroy`. [`el/carousel/carousel.js`](el/carousel/carousel.js#L278)
- [ ] **Carousel-Autoplaywert validieren:** Ungültige oder negative `--u2-carousel-slideshow-speed` werden zu 0-ms-Timern und können dauerhaft Slides/Events feuern. Min/Max, Einheiten und Reduced Motion definieren. [`el/carousel/carousel.js`](el/carousel/carousel.js#L237)
- [ ] **Carousel-Syncwert nicht in unescaped Selektor interpolieren:** Quotes/Sonderzeichen im `sync`-Attribut werfen bei jedem Slide `querySelectorAll`-Fehler. `CSS.escape()` oder Filterung über gelesene Attribute nutzen. [`el/carousel/carousel.js`](el/carousel/carousel.js#L354)
- [ ] **Parallax-BG-Stage nullsicher und reversibel verwalten:** `offsetParent` kann null sein; Body erhält dauerhaft inline `position:relative`, auch nach Entfernen der letzten Instanz. Stage explizit auflösen und Originalstyle wiederherstellen. [`el/parallax-bg/parallax-bg.js`](el/parallax-bg/parallax-bg.js#L113)
- [ ] **Parallax-BG-Frameguard aktivieren:** `positionizing` wird nie true gesetzt; Scroll/Wheel/Resize können pro Instanz mehrere RAFs für denselben Frame planen. Zentral einen einzigen Read/Write-Durchgang schedulen. [`el/parallax-bg/parallax-bg.js`](el/parallax-bg/parallax-bg.js#L184)
- [ ] **Parallax-BG auf Speed-/Stage-/Resizeänderungen reagieren:** Speed wird nur im Konstruktor gelesen, Stage nur beim Connect; Layoutänderungen des einzelnen Containers werden nicht direkt beobachtet. Validierung und per-Instanz ResizeObserver/Refresh ergänzen. [`el/parallax-bg/parallax-bg.js`](el/parallax-bg/parallax-bg.js#L109)
- [ ] **System-Hotkey ohne vorhandenes Element nicht crashen lassen:** Ein globaler `s`-Keydown dereferenziert zweimal `document.querySelector('u2-system')`; bloßes Importieren des Moduls ohne Element erzeugt Laufzeitfehler beim Tippen. Ziel einmal lesen und null prüfen. [`el/system/system.js`](el/system/system.js#L73)
- [ ] **System-Demo-Abhängigkeiten und Fetch abwarten:** Styler-Import und Demo-HTML-Fetch haben keinen Ready-/Status-/Errorpfad; frühes Öffnen kann ein noch nicht definiertes Element ansprechen oder Fehler-HTML injizieren. [`el/system/system.js`](el/system/system.js#L8)

## Ergänzungen aus dem zweiten Durchgang – P1 Interaktion und Accessibility

- [ ] **Contextmenu-Scoped-Shortcut-Ziel korrigieren:** `!event.target.closest(selector)` speichert einen Boolean; bei einem Match erhält die Action immer `documentElement` statt des passenden Elements. Das tatsächliche `closest()`-Resultat weiterreichen. [`js/contextmenu/contextmenu.js`](js/contextmenu/contextmenu.js#L95)
- [ ] **Contextmenu-Texte ohne `innerHTML` erstellen:** Label, Icon und Shortcut werden ungeescaped in Buttonmarkup interpoliert. Textfelder via `textContent`, bewusstes Icon-/HTML-API separat behandeln. [`js/contextmenu/contextmenu.js`](js/contextmenu/contextmenu.js#L71)
- [ ] **Contextmenu-Keyboardpfade nullsicher und ARIA-konform machen:** `activeElement`/erster Button können fehlen; Home/End/Escape/Enter/Typeahead und roving tabindex sind unvollständig. Auch reine Custom-HTML-Menüs dürfen nicht crashen. [`js/contextmenu/contextmenu.js`](js/contextmenu/contextmenu.js#L261)
- [ ] **Contextmenu in kleinen Viewports clampen:** Es wird nur `Math.min` angewandt; ist das Menü größer als der Viewport, entstehen negative Top/Left-Werte. Mindestwert, Zoom und VisualViewport berücksichtigen. [`js/contextmenu/contextmenu.js`](js/contextmenu/contextmenu.js#L318)
- [ ] **Shortcut-Listener müssen unsubscribe liefern:** Jeder Aufruf hängt zwei anonyme Listener permanent an; dynamische Contextmenu-Items sind nicht entfernbar. AbortSignal/Disposer unterstützen. [`js/shortcut/shortcut.js`](js/shortcut/shortcut.js#L29)
- [ ] **Shortcut-Kombinationen modifierunabhängig erkennen:** Die Reihenfolge der Keydowns muss exakt der Stringreihenfolge entsprechen, obwohl `Ctrl+S` semantisch gleichzeitig ist. Modifier über Eventflags/Set vergleichen, Sequenzen separat modellieren. [`js/shortcut/shortcut.js`](js/shortcut/shortcut.js#L1)
- [ ] **Shortcuts bei Blur resetten und Editing-Kontext respektieren:** Verlorene Keyups lassen `activeIndex` hängen; Inputs, Textareas und Contenteditable sollten standardmäßig nicht globale Aktionen auslösen. [`js/shortcut/shortcut.js`](js/shortcut/shortcut.js#L34)
- [ ] **Dialog-Form auch bei Enter korrekt auswerten:** FormData/Validity werden nur im OK-Button-Click berechnet. Native Submit per Enter kann den `method=dialog`-Dialog schließen, ohne den Wert zu setzen. Einen `submit`-Handler als einzige Commit-Stelle verwenden. [`js/dialog/dialog.js`](js/dialog/dialog.js#L132)
- [ ] **Dialog-Promise bei externer Entfernung/Fehlern definieren:** Es löst nur auf `close`; entfernt `init` oder fremder Code das Element, bleibt es pending. `showModal()`-Fehler, AbortSignal und idempotentes Cleanup behandeln. [`js/dialog/dialog.js`](js/dialog/dialog.js#L50)
- [ ] **Tour wirklich als Modal/Fokusbereich auslegen:** Das Info-`dialog` wird nur mit `open=true` nichtmodal gezeigt; Tastaturfokus kann hinter das Spotlight wandern. Focus Trap bzw. `inert`-Management, Dialogsemantik und Screenreaderfluss ergänzen. [`js/Tour/Tour.js`](js/Tour/Tour.js#L98)
- [ ] **Tour-Zielverlust und Async-Fehler behandeln:** Entfernte Targets lassen Draw/Polling weiterlaufen; rejected Target/Content-Promises liefern keinen definierten Error-/Skip-/Stop-Pfad. [`js/Tour/Tour.js`](js/Tour/Tour.js#L11)
- [ ] **Tour-Overlay-Stile korrigieren:** `xposition` und `xinset` sind ungültige Fantasie-Properties; Overlaygeometrie hängt zufällig am Popover/Shadow-SVG statt an explizitem Layout. [`js/Tour/Tour.js`](js/Tour/Tour.js#L61)
- [ ] **Drag-State nach Ende löschen und RAF lokal binden:** Exportiertes `dragged` bleibt nach `dragend` stale; der Dragstart-RAF greift auf die mutable globale Variable zu und kann bei einem neuen Drag das falsche Element markieren. [`js/drag/drag.js`](js/drag/drag.js#L4)
- [ ] **Drop-Indicator kollisionsfrei/lifecycle-sicher machen:** Beide Implementierungen teilen die globale ID `drop-indicator`, hängen an `document.body` und können fremde Nodes übernehmen. Pro Root kapseln, API zusammenführen und Popover-Support fallbacken. [`js/drag/indicateDrop.js`](js/drag/indicateDrop.js#L14) [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L331)
- [ ] **History-Patching composable und rückgängig machen:** `url-change-event.js` überschreibt globale `pushState`/`replaceState` dauerhaft und kann mit Navigator, Testframeworks oder anderen Wrappern kollidieren. Install/Destroy, Symbol-Markierung und Originaldescriptor erhalten. [`js/navigator/url-change-event.js`](js/navigator/url-change-event.js#L2)

## Ergänzungen aus dem zweiten Durchgang – P2 Dokumentation und Wartbarkeit

- [ ] **Bytes-README aus der Number-Kopie berichtigen:** Dokumentiert Currency/Percent/Unit-Features, die `u2-bytes` nicht implementiert; auch die Digits-Beschreibung ist falsch. Beispiele und Manifest aus echter API ableiten. [`el/bytes/README.md`](el/bytes/README.md#L22)
- [ ] **Time-Default zwischen Code und README angleichen:** README nennt `mode=short`, Implementierung setzt `long`. Einen Default festlegen und per Manifest-/Beispieltest absichern. [`el/time/time.js`](el/time/time.js#L76) [`el/time/README.md`](el/time/README.md#L58)
- [ ] **Doppelte Drop-Indicator- und Unit-Konverter zusammenführen:** Ähnliche, bereits auseinanderlaufende Implementierungen existieren in Drag/Dropzone sowie Masonry/weiteren Layoutmodulen. Gemeinsame kleine Helfer mit Root-/Writing-Mode-Tests verwenden. [`js/drag/indicateDrop.js`](js/drag/indicateDrop.js#L14) [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L331) [`el/masonry/masonry.js`](el/masonry/masonry.js#L55)
- [ ] **Autoformat-Doppelpass entfernen:** Long-Text/Header-Analyse läuft in zwei nahezu identischen Schleifen; der zweite Pfad berechnet bei leerer Werteliste `Math.max(...[])`. Einmal klassifizieren, dann einmal anwenden. [`el/table/ext/autoFormatTable.js`](el/table/ext/autoFormatTable.js#L185)
- [ ] **Global installierte Utility-Module als Instanzen/Controller anbieten:** Contextmenu, Drag, Behavior und Intersect installieren bei Import sofort Documentlistener/DOM und haben keinen Destroy-/Root-Vertrag. Das erschwert Tests, iframes und mehrere App-Roots. [`js/contextmenu/contextmenu.js`](js/contextmenu/contextmenu.js#L253) [`js/drag/drag.js`](js/drag/drag.js#L4) [`attr/intersect/intersect.js`](attr/intersect/intersect.js#L5)
- [ ] **Tour-Polling durch zielgerichtete Observer ersetzen:** Zusätzlich zu Scroll/Resize läuft dauerhaft ein 200-ms-Interval. Target/Info mit ResizeObserver beobachten und Updates in einem Frame bündeln. [`js/Tour/Tour.js`](js/Tour/Tour.js#L93)
- [ ] **`transition: all` aus bewegten Overlays entfernen:** Dropzone/Spotlight animieren damit unbeabsichtigt jede Property und erschweren Reduced Motion. Nur transform/position/size gezielt animieren und Mediaquery respektieren. [`attr/dropzone/dropzone.js`](attr/dropzone/dropzone.js#L359) [`js/Tour/Tour.js`](js/Tour/Tour.js#L77)

## Bereits ausgeführte Audit-Checks

- [x] Zweiter vertiefter statischer Sweep über Observer/Positionierung, Attribute, Form-/Layout-Elemente, Dialog/Contextmenu/Tour, Chart/Code/Video und Cookiebanner; neue Punkte mit dem ersten Durchgang dedupliziert.
- [x] 825 versionierte Dateien und alle Hauptbereiche (`attr`, `class`, `css`, `el`, `js`, `theme`, `u2`) inventarisiert.
- [x] Alle aktuell vorhandenen JSON-Dateien erneut geparst; kein Syntaxfehler verblieben.
- [x] Katalog gegen Modulverzeichnisse und `css/js`-Flags abgeglichen; committed Bestand ist konsistent, die laufende uncommitted RTE2-Arbeit noch nicht generiert.
- [x] 275 statische relative Produktions-Imports auf Ziel-Existenz geprüft; der aktive berechnete `u2-out`-Pfad ist falsch (ein Treffer im alten RTE liegt innerhalb eines auskommentierten Blocks).
- [x] Lokale Markdown-Links auf Existenz geprüft; keine fehlenden lokalen Ziele gefunden.
- [x] Kernmodule mit `deno check` geprüft.
- [x] Root-Demo per HTTP in Chromium gerendert und Konsolenmeldungen ausgewertet; kein vollständiger Cross-Browser-/Interaktionstest durchgeführt.
